/**
 * LeetCode 084: 移动零 (Move Zeroes)
 *
 * 题目描述：
 * 给定一个数组 nums，编写一个函数将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。
 * 请注意，必须在不复制数组的情况下原地对数组进行操作。
 *
 * 核心思想：
 * 双指针技巧 - 快慢指针分离零元素和非零元素
 *
 * 算法原理：
 * 1. 使用双指针：slow指向下一个非零元素应该放置的位置
 * 2. fast指针遍历整个数组寻找非零元素
 * 3. 当找到非零元素时，将其移动到slow位置
 * 4. 最后将剩余位置填充为0
 */

// TODO: 待实现
// 预计包含以下解法：
// 1. 双指针法（推荐）
// 2. 双指针优化版
// 3. 额外数组法
// 解决数组元素移动的经典问题

module.exports = {
    // 主要解法将在这里实现
};
